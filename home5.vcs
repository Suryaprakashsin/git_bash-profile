what is git status?
the git status command dusplays the state of the working directory and the staging area.
ut lests you see which changes have been staged, which haven't and which files aren't being
tracked by git. git status doest not show you any information regarding the committed project history.

Git Status 
The git status command will display the current status of the repository.